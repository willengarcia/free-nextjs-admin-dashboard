# Server-side address of the Spring Boot API. Do not prefix this variable with NEXT_PUBLIC_.
API_BASE_URL=http://localhost:8080

# Optional name displayed in the administrative header. Falls back to the logged-in user's name.
NEXT_PUBLIC_ADMIN_DISPLAY_NAME=Administracao
